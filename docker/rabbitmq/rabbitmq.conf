# Load exchange, queue, and user definitions on startup.
# See docker/rabbitmq/definitions.json for the full configuration.
management.load_definitions = /etc/rabbitmq/definitions.json

# Allow guest user to connect from any host (required for Docker networking).
loopback_users = none
